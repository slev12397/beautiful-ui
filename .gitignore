# deps
/node_modules

# next
/.next/
/out/

# misc
.DS_Store
*.pem
npm-debug.log*

# typescript
*.tsbuildinfo
next-env.d.ts
.vercel
.env*
!.env.example

# private reference material (moved out of repo)
/Refs

# local agent / workspace tooling — never publish
/.agents
/.claude
/.context
/skills-lock.json

# easter-egg video — kept on the deploy (via .vercelignore) but not published
/public/subway-surfers.mp4

# generated by scripts/build-registry.mjs (runs as part of `npm run build`)
/public/r
